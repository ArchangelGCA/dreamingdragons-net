# Migrated to Bun 1.4 — this file is kept for compatibility but no longer enforces Node engines.
# Bun uses `bunfig.toml` and `packageManager: bun@1.4.0` in package.json.
# engine-strict is intentionally disabled; Bun validates via `engines.bun`.
engine-strict=false
